Programming?
giving instructions to computer to perform a specific task

C programming
it is a structured, high level programming language

structured - paradigms 1.functional(structural) 2.OOP
high level - 

machine language - 0's and 1's - binary - 10010 10101 11010 - 3+4
assembly - nuemonics & symbols - add 3,4
high level - c - human readable - 3+4

compiled programming lang.
- whole code is checked for error and if there are no errors then an executable file will be created


C compiler - msys2.org
Vs code - 

gcc

2>3 - F - 0
2<3 - T - 1

In C true is represented using 1 and false is represented using 0

All non zero numbers are True


2>3 && 2<4 - F - 0
2>3 || 2<4 - T - 1

0 && 1 - 0

2+3
unary - !,++,--,~,sizeof
binary - &&, ||
ternary - ?:


#include <stdio.h>

// Uncomment the next line to turn on debugging features
#define DEBUG_MODE 

int main() {
    int total_score = 450;

    #ifdef DEBUG_MODE
        // This block will compile ONLY because DEBUG_MODE is defined
        printf("[DEBUG] Current score variable holds: %d\n", total_score);
    #endif

    printf("Application finished executing successfully.\n");
    return 0;
}


#include <stdio.h>

// If no previous file or compiler setting defined the buffer size, set a default
#ifndef BUFFER_SIZE
    #define BUFFER_SIZE 1024
#endif

int main() {
    printf("The buffer capacity is configured to: %d bytes\n", BUFFER_SIZE);
    return 0;
}



#include <stdio.h>

#define WINDOWS_OS

int main() {
    #ifdef WINDOWS_OS
        printf("Running on a Windows operating system environment.\n");
    #else
        printf("Running on a Linux or alternative operating system environment.\n");
    #endif
    return 0;
}




printf("The square of %d is %d\n", number, a*a);
a+b\
+c+d\
